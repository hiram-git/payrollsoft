{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "inputs": ["src/**", "index.html", "vite.config.ts", "capacitor.config.ts", "package.json"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
