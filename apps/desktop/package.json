{
  "name": "@payroll/desktop",
  "version": "0.0.1",
  "private": true,
  "description": "Tauri 2 desktop shell that wraps the PayrollSoft web app (Windows v1)",
  "scripts": {
    "dev": "node scripts/dev-guard.mjs dev",
    "build": "node scripts/dev-guard.mjs build",
    "build:dist": "node scripts/build-dist.mjs",
    "dev:force": "tauri dev",
    "build:force": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
